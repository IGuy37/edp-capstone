import React from "react";

export default function Subordinate(props){

    //make this link to an Employee component with the corresponding data
    return(
        <>
            <div>{props.name}</div>  
        </>
    );    
}